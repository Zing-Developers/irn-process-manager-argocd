apiVersion: v1
kind: Service
metadata:
  name: Igrp-process-studio-backend-service
  labels:
    app: Igrp-process-studio-backend
spec:
  selector:
    app: Igrp-process-studio-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP