apiVersion: apps/v1
kind: Deployment
metadata:
  name: igrp-process-management-frontend
  labels:
    app: igrp-process-management-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: igrp-process-management-frontend
  template:
    metadata:
      labels:
        app: igrp-process-management-frontend
    spec:
      containers:
        - name: igrp-process-management-frontend
          image: irnzing/irn:igrp-process-management-frontend
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
          envFrom:
            - configMapRef:
                name: irn-process-management-frontend-conf
            - secretRef:
                name: irn-process-management-frontend-secret