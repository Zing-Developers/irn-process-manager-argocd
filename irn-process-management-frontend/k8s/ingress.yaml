apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: irn-process-management-frontend-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: "to_be_defined"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: irn-process-management-frontend-service
                port:
                  number: 8080
