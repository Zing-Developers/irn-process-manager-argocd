apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: igrp-platform-access-management-backend-ingress
  namespace: igrp-platform-access-management-backend
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: "to_be_defined"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: igrp-platform-access-management-backend-service
                port:
                  number: 8080
