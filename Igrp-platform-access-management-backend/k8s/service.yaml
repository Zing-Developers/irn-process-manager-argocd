apiVersion: v1
kind: Service
metadata:
  name: Igrp-platform-access-management-backend-service
  labels:
    app: Igrp-platform-access-management-backend
spec:
  selector:
    app: Igrp-platform-access-management-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
