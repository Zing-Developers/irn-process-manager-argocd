apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: Igrp-process-runtime-backend-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: "to_be_defined"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: Igrp-process-runtime-backend-service
                port:
                  number: 8080