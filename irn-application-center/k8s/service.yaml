apiVersion: v1
kind: Service
metadata:
  name: irn-application-center-service
  labels:
    app: irn-application-center
spec:
  selector:
    app: irn-application-center
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
